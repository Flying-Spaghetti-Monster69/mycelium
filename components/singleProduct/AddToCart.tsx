import { Button } from "../ui/button";
//{ id }: { id: string }
const AddToCart = () => {
  return (
    <Button className="capitalize mt-8" size="lg">
      Add to cart
    </Button>
  );
};

export default AddToCart;
